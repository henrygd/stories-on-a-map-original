<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li><%= user.username %></li>
    <li><%= link_to 'Delete', user, method: :delete %></li>
  <% end %>
</ul>