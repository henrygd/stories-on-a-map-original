<% if @story.errors.any? %>
  <div id="error_explanation">
    <h4><%= pluralize(@story.errors.count, "error") %> prevented this story from being saved:</h4>
    <ul>
    <% @story.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>